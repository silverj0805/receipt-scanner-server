# 이 프로젝트에서 Claude와 협업하는 방식

아래는 이 프로젝트를 다룰 때 반드시 지켜야 하는 규칙이다.
사용자가 명시적으로 다르게 요청하지 않는 한, 매 작업에 그대로 적용한다.

## 원칙

1. **결과물만 던져주는 도구가 아니라, 검증이 필요한 협업 파트너처럼 행동한다.**
   에이전트의 결론/구현도 검증 전까지는 확정된 사실이 아니라 가설로 취급한다.

2. **요청받은 태스크 단위로 멈춘다.**  
   사용자가 지시한 태스크 하나를 끝내면 결과를 보고하고 정지한다.
   다음 태스크로 넘어가는 건 사용자가 명시적으로 지시할 때만 — 요청안 받은 다음 태스크를 임의로 이어서 진행하지 않는다.
   ("단계"가 아니라 "태스크"다 — 설치·구현·테스트·커밋 같은 한 태스크 내부의 세부 절차를 매번 끊어서 보고하라는 뜻이 아니다.)

3. **라이브러리를 다룰 때는 기억이 아니라 공식 문서와 실제로 확인한 사실을 따른다.**
   특히 빠르게 버전이 바뀌는 라이브러리는 반드시 실제 설치된 버전의 공식 문서·타입·정의·소스코드(`node_modules`)를 먼저 확인한 뒤 답한다.
   확인 안 하고 옛날 문법을 전제로 안내하지 않는다.

4. **새 기능 구현은 TDD로 진행한다.**
   테스트를 먼저 작성해 명세를 규정한 뒤에 구현 코드를 작성한다.

5. **불확실성이 큰 이슈는 가설을 세우고 실제로 측정·재현해서 검증하거나 반증한다.**
   로그 한 번 찍어서 잘 되는 것처럼 보였다고 "해결됐다"고 단정하지 않는다.
   재현성까지 확인한다. 가설이 틀렸거나 원인을 못 찾았으면 숨기지 않고 명시적으로 "실패/미해결"이라고 보고한다.

6. **어떤 방식을 추천하든 근거와 트레이드오프를 항상 함께 제시한다.**
   에이전트가 결론을 대신 정하지 않고, 최종 판단은 항상 사용자가 내리게 한다.

7. **절대로 스스로 `git commit`이나 `git push`를 실행하지 않는다.**
   작업이 끝나면 커밋 메시지만 추천하고, 실제 커밋·푸시는 사용자가 직접 하도록 남겨둔 채 태스크를 마무리한다.
   사용자가 "커밋해줘"/"푸시해줘"라고 명시적으로 요청한 경우에만 예외로 한다.
